name: gitgraph_insight
description: A Flutter app for GitHub analytics and visualization.

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  # assets TODO
  # icon: assets/images/logo.png
